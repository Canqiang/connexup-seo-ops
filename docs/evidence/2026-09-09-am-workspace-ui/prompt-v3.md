修改本项目已有的 index.html（AM 工作台界面原型 v2），保留导航、配色、卡片、戳记、按钮和工作单布局，只做减法和补关键状态。仍是静态原型集，桌面 1280px 起，只用 seo-ops 的 tokens.css 变量，无外部依赖。9 个 `<section id>` 不变：am-home、am-merchant-space、am-gbp-work-order、am-task-detail-human、am-dispatch-drawer、am-team、am-memory、am-model-cost、am-reports；在 am-gbp-work-order 之后新增第 10 个区块 `am-exceptions`。页面顶部说明改为三条原则：「Agent 主动推进，AM 按职责参与」「Plan 授权工作范围，内容审批授权具体产物」「系统用证据判断完成」。页面顶部加一条灰色横幅「演示数据 · 所有数字为示例，已校验自洽」。

## 一、首页 am-home：提高密度、单一归属、解释原因

1）四组事项按「风险 → 截止 → 阻塞的下游数」排序，每张卡固定三行：结论 / 影响 / 当前有效操作（一个主按钮）；其他操作收进「更多 ▾」；依据折叠。
2）「需要我决定」只剩 3 件：「审阅草稿 · brunch 帖子 v2 · George」（标注「R3 · 公开内容需逐条审批」）、「整体批准变更申请 · Local Falcon 扫描 120 credits 超出 Plan 额度 100」（标注「R4 · Plan #12 付费扫描额度 100 credits，本周期已用 0，本次 120」，主按钮「整体批准这 2 项」）、「上次发布结果未知，需要你核验 · Only Bear」（标注「R5 · 系统已读回 3 次，最后一次 09-09 11:42，未找到精确匹配」，主按钮「开始核验」）。
3）删除「Local Falcon 扫描将消耗 40 credits」审批卡；改到「仅知会」：「Local Falcon 扫描 40 credits 已在 Plan #12 额度 100 内自动执行 · 已用 40 / 100」。
4）「分派给我」2 件：「确认 Weekday Brunch Set 的价格与有效期」（R2 · 阻塞 1 个草稿）、「审阅第 37 周周报草稿 · 可可小卤」；不再出现任何审核草稿的重复卡。
5）「需要关注」1 件：「可可小卤 · GSC 授权已过期 · 责任人 xander · 动作 重新授权 · 影响 3 个同步任务（内部草稿不受影响）」。
6）团队 rail 默认收起成一行「团队 · 5 个 Agent 工作中 1 · 展开 ›」。

## 二、工作单 am-gbp-work-order：去重

- 右侧「现在需要谁做什么」和「负责人」两张卡合并成一张「当前」卡：只写「第 ② 步 审核 · xander · 截止 09-10 12:00 · 主操作：批准这一版 / 退回并说明」；流水线里每步的负责人保留，侧栏不再重复列全部负责人。
- 第 ② 步的操作区只保留「批准这一版」为主按钮，「退回并说明」次按钮，其他（暂时受阻 / 请求协助 / 转交 / 取消）收进「更多 ▾」。
- 批准确认框文案改为：「批准后发布 Agent 会把帖子 v2 写入 Google 门店 2020 Broadway。发布后不能退回草稿，修改或撤下需要新建补偿任务。确认？」
- 批准绑定行：「本次批准绑定：正文 b4de70ec… · 图片 sha256 91af… · CTA 了解更多 → https://georgemerchant.example/menu · 门店 2020 Broadway · 发布时间窗 09-09 至 09-16」。

## 三、新增 am-exceptions：四组异常状态并排

标题「异常恢复 · 四组状态」，说明「每张异常卡回答四件事：谁处理、系统已尝试什么、下一步是什么、什么时候升级。默认只限制受影响的任务。」四张大卡横向两列排：

E1「退回 → 修改 → 新审核」：工作单 WO-1204 版本链：草稿 v1（已完成 09-08）→ 审核 v1（已退回 · xander · 理由「图片来源未授权」）→ 草稿 v2（已完成 09-09 10:12）→ 审核 v2（分派给你）。卡底：谁处理「内容 Agent 已重做 · 你再审」/ 系统已尝试「自动建草稿 v2 与审核 v2，退回理由已进候选经验」/ 下一步「审阅草稿 v2」/ 升级「同一工作单退回 ≥ 3 次进需要处理」。

E2「批准后产物变化 → 批准失效」：横幅 warning「批准已失效：图片被替换（sha256 变化）。发布 Task 已停在待授权，已新建审核任务。」谁处理「xander 重新审核」/ 系统已尝试「停止发布、失效原批准 #A-88、建审核 v3」/ 下一步「审阅 v3」/ 升级「48 小时无人处理进需要处理」。

E3「发布超时 → 自动核验 → 人工介入」：时间线：11:02 发布 Agent 超时（未取得 post_id）→ 11:03 自动核验 #1 未匹配 → 11:08 #2 未匹配 → 11:23 #3 未匹配 → 状态「需要处理 · 不会自动重发」。谁处理「xander 核验」/ 系统已尝试「读回 3 次」/ 下一步「开始核验 · 查看读回列表」/ 升级「重读用尽即进需要我决定（已升级）」。

E4「输入任务取消 → 下游重新规划」：「确认 Weekday Brunch Set 价格」被取消（原因：商户暂不推价格）→ 草稿 v2 显示「等待依赖」→ Orchestrator 提案（范围内已应用）：「解除价格依赖 · 本版不含价格 · 含价格更新帖另建工作单」。谁处理「Orchestrator 已处理，你只需知会」/ 系统已尝试「发重新规划事件、应用解除依赖」/ 下一步「草稿 v2 继续准备」/ 升级「24 小时无提案进需要处理」。

右下角一张「影响范围分级」小表：任务级（默认）/ 商户级（凭证失效、预算耗尽）/ 项目级（提供方不可用、绑定漂移），各一行例子。

## 四、派单抽屉 am-dispatch-drawer

范围外那组卡改成 R4 例子：「新增 Local Falcon 扫描 · 120 credits」「扫描后重算关键词监测集」，组顶部一行「R4 · Plan #12 付费扫描额度 100 credits，本周期已用 40，本次 120，超出 60」，主按钮「整体批准这 2 项」。范围内那组加一行「R1 · 在 Plan #12 允许的『GBP 帖子发布（逐条审批）』与『只读同步』范围内」。

## 五、记忆 am-memory

三栏改名为「经营事实」「业务规则与偏好」「候选经验」，顶部说明「经营事实只从商户资料读，记忆不得覆盖；规则与偏好需 AM 确认并注明作用范围；候选经验只作提示，未确认不进入发布前事实检查。」候选经验里加一条「周三发帖效果更好」，戳记「策略假设 · 待数据验证 · 验证周期 4 周」，按钮「设为验证中」「拒绝」，说明「确认偏好适用，不等于证明策略有效」。Core AI 原生记忆卡的状态改为「未启用 · 隔离未验证」（中性灰，不是 warning），说明「未证明按商户隔离前，任何调用都不注入记忆」，按钮「查看验证清单」。底部一行度量：「重复错误 -40% · 一次通过率 71% → 83% · 单条内容成本 $0.42 → $0.31（近 4 周，演示数据）」。

## 六、模型与成本 am-model-cost

表格改成按「工作阶段」而不是按角色：行 = 编排与复杂诊断 / 证据冲突裁决 / 常规草稿与改写 / 格式整理 / 同步与计算 / 权限检查 / 发布与核验；列 = 档位（高能力 / 经济 / 确定性程序）、当前模型（如 deepseek-v4-pro / deepseek-v4-lite / 无 LLM）、升级条件（可判定，如「结构化输出连续 2 次不合格」；确定性行写「不适用」）、本月调用 / 升级次数。右侧「预算规则」卡改为三行成本：已消耗 $12.60 · 已预留 $3.20（4 个执行中任务）· 未结算 $0.90，合计 $16.70 / 额度 $40；规则「触发前检查 已消耗+已预留+未结算 ≤ 额度」「每任务升级 ≤ 1 次」「缺事实、缺权限不允许升级」。底部说明「角色与模型不永久绑定；切换不改变权限与验收标准」。

## 七、报告 am-reports

分三个 tab（并排静态展示三块）：「表现看板」（筛选条：商户 / 门店 / 期间 09-01 至 09-07 / 比较期 上周；三张指标卡 + 一条趋势占位）、「分析与 Audit」（沿用现有 Audit 报告卡，按钮「查看证据」「交给 Orchestrator 提计划」）、「冻结报告」（沿用三行列表，但第 37 周周报的导出列改为「导出草稿 PDF（带『草稿 · 未审核』标识）」，交付列「— · 正式交付需审核」；说明「生成文件、下载、对客户发送是三个动作，各自留记录」）。旁边一张「旧报告覆盖清单」小卡：6 行「旧报告项 → 新页面位置 → 已覆盖 / 未覆盖」，其中 2 行标「未覆盖」。

## 八、数字自洽

Plan #12 额度 100 credits、仅知会里已用 40、派单抽屉里已用 40 + 本次 120 超出 60；工作单版本链与 E1 一致；E3 的读回次数与首页 R5 卡一致；成本三态合计与额度一致。所有中文文案按上面原句使用。
